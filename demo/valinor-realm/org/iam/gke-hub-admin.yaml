apiVersion: iam.cnrm.cloud.google.com/v1beta1
kind: IAMPolicyMember
metadata:
  name: gke-hub-admin-valinor-realm
  namespace: config-control
spec:
  memberFrom:
    serviceAccountRef:
      name: valinor-realm
  role: roles/gkehub.admin
  resourceRef:
    kind: Project
    external: projects/valinor-realm