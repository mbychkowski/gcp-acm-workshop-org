apiVersion: iam.cnrm.cloud.google.com/v1beta1
kind: IAMPolicyMember
metadata:
  name: service-account-admin-valinor-realm
  namespace: config-control
spec:
  memberFrom:
    serviceAccountRef:
      name: valinor-realm
  role: roles/iam.serviceAccountAdmin
  resourceRef:
    kind: Project
    external: projects/valinor-realm