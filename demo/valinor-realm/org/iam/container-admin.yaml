apiVersion: iam.cnrm.cloud.google.com/v1beta1
kind: IAMPolicyMember
metadata:
  name: container-admin-valinor-realm
  namespace: config-control
spec:
  memberFrom:
    serviceAccountRef:
      name: valinor-realm
  role: roles/container.admin
  resourceRef:
    kind: Project
    external: projects/valinor-realm